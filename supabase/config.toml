# Minimal config so `supabase/` is recognized as a linked project by the
# Supabase CLI. This was NOT generated by `supabase init` (no CLI
# credentials were available in the session that created this file) --
# it only carries what `supabase link`/`db push` actually need. Run
# `supabase init` yourself if you also want the full local-dev-stack
# config (ports, auth/storage local settings, etc.) merged in; it will
# not touch this file's project_id or the migrations folder.
project_id = "ghojtefwuzubqguqcbwc"
