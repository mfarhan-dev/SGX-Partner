import '../../../shared/widgets/placeholder_screen.dart';

class NotificationsScreen extends PlaceholderScreen {
  const NotificationsScreen({super.key})
    : super(
        title: 'Notifications',
        description: 'Role-safe notification list with typed deep links.',
      );
}
