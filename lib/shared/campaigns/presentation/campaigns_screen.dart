import '../../../shared/widgets/placeholder_screen.dart';

class CampaignsScreen extends PlaceholderScreen {
  const CampaignsScreen({super.key})
    : super(
        title: 'Campaigns',
        description:
            'Role-filtered campaign list without user-facing progress bars.',
      );
}
