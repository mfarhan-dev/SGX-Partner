import '../../../shared/widgets/placeholder_screen.dart';

class ProductsScreen extends PlaceholderScreen {
  const ProductsScreen({super.key})
    : super(
        title: 'Products',
        description:
            'Browse-only B2B catalog. Prices, stock, cart, and supplier data are intentionally hidden.',
      );
}
