import '../../../shared/widgets/placeholder_screen.dart';

class AccountUnavailableScreen extends PlaceholderScreen {
  const AccountUnavailableScreen({super.key})
    : super(
        title: 'Account Unavailable',
        description: 'This phone number is not active for SGX Partners.',
      );
}
