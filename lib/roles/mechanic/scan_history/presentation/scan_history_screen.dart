import '../../../../shared/widgets/placeholder_screen.dart';

class ScanHistoryScreen extends PlaceholderScreen {
  const ScanHistoryScreen({super.key})
    : super(
        title: 'Scan History',
        description:
            'Confirmed scan history only. No pending offline scan queue exists.',
      );
}
