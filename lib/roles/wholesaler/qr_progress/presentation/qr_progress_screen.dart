import '../../../../shared/widgets/placeholder_screen.dart';

class QrProgressScreen extends PlaceholderScreen {
  const QrProgressScreen({super.key})
    : super(
        title: 'QR Progress',
        description:
            'Aggregate reward QR progress. Cards are not tappable and there is no QR detail route.',
      );
}
